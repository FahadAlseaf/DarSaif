import { projectSchema } from "./project";
import { teamMemberSchema } from "./teamMember";
import { serviceSchema } from "./service";

export const schemaTypes = [projectSchema, teamMemberSchema, serviceSchema];
